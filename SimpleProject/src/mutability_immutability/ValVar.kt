package mutability_immutability

fun main(){
    // val = value (cannot be changed)
    val name = "Victor Hugo"
    // name = "Victor Oliveira" (val cannot be reassigned)

    // var = variable (can be changed)
    var age =  28
    age = 29
}